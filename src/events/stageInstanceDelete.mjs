import log from '../log.mjs';
import { getMsg } from '../locales.mjs';

// Event handler for stageInstanceDelete
export default async function (stageInstance) {
    log.debug('stageInstanceDelete', { stageInstance });
}
